{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) case \"$f\" in */liulian/*|*/tests/*|*/plugins/*) uvx ruff format \"$f\" && uvx ruff check --fix \"$f\" ;; esac;; esac; }"
          }
        ]
      }
    ]
  }
}
